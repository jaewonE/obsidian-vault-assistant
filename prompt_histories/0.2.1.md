good. new let do version 0.2.1.

Requirements:
The NotebookLM operation process can be broadly divided into three steps.

1. Search documents based on BM25 and select documents to upload.
2. Upload the selected documents to NotebookLM.
3. Wait for the response.

Add a UI to display the progress for these three steps (including which document is currently being uploaded in step 2).